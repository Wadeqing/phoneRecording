package com.sinocall.phonerecordera.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.sinocall.phonerecordera.dao.ConstactBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CONSTACT_BEAN".
*/
public class ConstactBeanDao extends AbstractDao<ConstactBean, Long> {

    public static final String TABLENAME = "CONSTACT_BEAN";

    /**
     * Properties of entity ConstactBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property Pinyin = new Property(2, String.class, "pinyin", false, "PINYIN");
        public final static Property Jianpin = new Property(3, String.class, "jianpin", false, "JIANPIN");
        public final static Property SortKey = new Property(4, String.class, "sortKey", false, "SORT_KEY");
        public final static Property Mobile = new Property(5, String.class, "mobile", false, "MOBILE");
    }


    public ConstactBeanDao(DaoConfig config) {
        super(config);
    }
    
    public ConstactBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CONSTACT_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: id
                "\"NAME\" TEXT," + // 1: name
                "\"PINYIN\" TEXT," + // 2: pinyin
                "\"JIANPIN\" TEXT," + // 3: jianpin
                "\"SORT_KEY\" TEXT," + // 4: sortKey
                "\"MOBILE\" TEXT NOT NULL );"); // 5: mobile
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CONSTACT_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ConstactBean entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String pinyin = entity.getPinyin();
        if (pinyin != null) {
            stmt.bindString(3, pinyin);
        }
 
        String jianpin = entity.getJianpin();
        if (jianpin != null) {
            stmt.bindString(4, jianpin);
        }
 
        String sortKey = entity.getSortKey();
        if (sortKey != null) {
            stmt.bindString(5, sortKey);
        }
        stmt.bindString(6, entity.getMobile());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ConstactBean entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String pinyin = entity.getPinyin();
        if (pinyin != null) {
            stmt.bindString(3, pinyin);
        }
 
        String jianpin = entity.getJianpin();
        if (jianpin != null) {
            stmt.bindString(4, jianpin);
        }
 
        String sortKey = entity.getSortKey();
        if (sortKey != null) {
            stmt.bindString(5, sortKey);
        }
        stmt.bindString(6, entity.getMobile());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public ConstactBean readEntity(Cursor cursor, int offset) {
        ConstactBean entity = new ConstactBean( //
            cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // pinyin
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // jianpin
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // sortKey
            cursor.getString(offset + 5) // mobile
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ConstactBean entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setPinyin(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setJianpin(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setSortKey(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setMobile(cursor.getString(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ConstactBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ConstactBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ConstactBean entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
